# GitHub Repo Setup TODO

## Completed Steps:
1. Initialize Git repository (`git init`) ✅

## Pending Steps:
2. Add all files (`git add .`)
3. Make initial commit (`git commit -m "Initial commit"`)
4. Create GitHub repo using `gh repo create SCD_WD_2 --public`
5. Add remote origin and push (`git push -u origin main`)
6. Verify and complete (`attempt_completion`)
